{
  "name": "electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "electron": "electron .",
    "electron-dev": "cross-env ELECTRON_DEV=true npx electron .",
    "dev-all": "concurrently \"cd .. && python manage.py runserver\" \"cd ../../app && npm start\" \"wait-on tcp:3000 && cross-env ELECTRON_DEV=true npx electron .\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.0.0",
    "electron": "^39.1.1",
    "wait-on": "^7.0.1"
  }
}
